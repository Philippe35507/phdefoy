---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

// Import d'un fichier Markdown pour le bloc de texte de la home
interface MarkdownModule {
  Content: any;
  frontmatter?: any;
}
const mdModules = import.meta.glob<MarkdownModule>('../content/blog/Autres/apprendre-espagnol-et-russe-methodes-efficaces-pour-francophones.md');
const mdModule = await mdModules['../content/blog/Autres/apprendre-espagnol-et-russe-methodes-efficaces-pour-francophones.md']();
const { Content } = mdModule;

// Schema.org Person JSON-LD
const personSchema = {
  "@context": "https://schema.org",
  "@type": "Person",
  "name": "Philippe de Foy",
  "url": "https://phdefoy.com",
  "description": SITE_DESCRIPTION,
  "knowsLanguage": ["fr", "es", "ru", "en"],
  "knowsAbout": ["Langue russe", "Langue espagnole", "Apprentissage des langues"],
  "sameAs": [
    "https://x.com/phdefoy",
    "https://www.facebook.com/phdefoy",
    "https://www.instagram.com/phdefoy",
    "https://www.youtube.com/@phdefoy"
  ]
};
---

<!doctype html>
<html lang="fr">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <!-- CSS spécifiques -->
    <link rel="stylesheet" href="/styles/cookie-banner.css" />
<!-- Balises hreflang pour le multilingue -->
<link rel="alternate" hreflang="fr" href="https://phdefoy.com/" />
<link rel="alternate" hreflang="es" href="https://phdefoy.com/descargas/" />
<link rel="alternate" hreflang="x-default" href="https://phdefoy.com/" />
    <!-- Schema.org Person -->
    <script type="application/ld+json" set:html={JSON.stringify(personSchema)} />
  </head>

  <body class="page-bg home">
    <Header />
    <main>
      <!-- ===== SECTION WELCOME ===== -->
      <section class="welcome-container">
        <h2>Bienvenue - Cours et ressources pour apprendre le Russe et l'Espagnol</h2>

        <a href="/telechargements-audio-gratuits/" class="cta-audio">
          <span class="icon-headphones" aria-hidden="true">🎧</span>
          <span class="cta-text">Téléchargez nos audios gratuits</span>
          <span class="cta-arrow">→</span>
        </a>
      </section>

      <!-- ===== SECTION TEXTE (Markdown dédié à la home) ===== -->
      <section class="blog-section">
        <div class="blog-container">
          <Content />
        </div>
      </section>


      <style>
        /* ===== THEME GLOBAL ===== */
        body.page-bg {
          min-height: 100vh;
          background: linear-gradient(135deg, #12141f 0%, #1a1c2b 40%, #23254a 100%);
          margin: 0;
          font-family: var(--font-sans);
        }
        main { padding-top: 0; margin-top: -20px; }

        /* ===== SECTION BLOG (bloc Markdown) ===== */
        .blog-section {
          max-width: 900px;
          margin: 2rem auto;
          padding: 0 2rem;
          color: #f8fafc;
        }
        .blog-container {
          background: rgba(30, 41, 59, 0.6);
          backdrop-filter: blur(10px);
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 16px;
          padding: 3rem;
          margin-bottom: 4rem;
          line-height: 1.7;
        }
        .blog-container :global(h1) {
          font-size: clamp(2rem, 5vw, 2.5rem);
          color: #f8fafc;
          font-weight: 700;
          letter-spacing: -0.02em;
          margin-bottom: 1.5rem;
          text-align: center;
          background: linear-gradient(135deg, #60a5fa, #34d399);
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          background-clip: text;
        }
        .blog-container :global(h2) {
          font-size: 1.5rem;
          color: #60a5fa;
          font-weight: 600;
          margin-top: 2.5rem;
          margin-bottom: 1rem;
          border-left: 4px solid #60a5fa;
          padding-left: 1rem;
        }
        .blog-container :global(p) { margin-bottom: 1.5rem; color: #cbd5e1; font-size: 1.1rem; }
        .blog-container :global(strong) { color: #f1f5f9; font-weight: 600; }
        .blog-container :global(em) { color: #94a3b8; font-style: italic; }
        .blog-container :global(p:last-child) {
          text-align: center; font-style: italic; color: #10b981; font-size: 1.2rem; margin-top: 2rem;
          padding: 1.5rem; background: rgba(16, 185, 129, 0.1); border: 1px solid rgba(16, 185, 129, 0.3);
          border-radius: 12px;
        }

        /* ===== SECTION WELCOME ===== */
        .welcome-container {
          max-width: 800px;
          margin: 3rem auto 3rem;
          padding: 0rem 2rem;
          text-align: center;
          color: #f8fafc;
        }
        .welcome-container h2 {
          font-size: clamp(1.75rem, 4vw, 2.5rem);
          color: #f8fafc;
          font-weight: 700;
          letter-spacing: -0.02em;
          margin-top: 4rem;
          margin-bottom: 2rem;
          text-align: center;
        }

        .cta-audio {
          display: inline-flex;
          align-items: center;
          gap: 1rem;
          padding: 1.25rem 2rem;
          background: rgba(30, 41, 59, 0.6);
          backdrop-filter: blur(10px);
          border: 1px solid rgba(255, 255, 255, 0.1);
          border-radius: 16px;
          text-decoration: none;
          color: #f8fafc;
          transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
          animation: pulse-glow 2.5s ease-in-out infinite;
        }
        .cta-audio:hover {
          transform: translateY(-4px);
          background: rgba(30, 41, 59, 0.8);
          border-color: rgba(96, 165, 250, 0.5);
          box-shadow: 0 12px 20px -5px rgba(0, 0, 0, 0.3);
          animation: none;
        }
        .icon-headphones {
          font-size: 2.5rem;
          filter: drop-shadow(0 4px 12px rgba(79, 142, 247, 0.3));
          animation: bounce-headphones 2s ease-in-out infinite;
        }
        .cta-text {
          font-size: 1.2rem;
          font-weight: 600;
        }
        .cta-arrow {
          font-size: 1.5rem;
          color: #60a5fa;
          transition: transform 0.2s;
          animation: arrow-move 1.5s ease-in-out infinite;
        }
        .cta-audio:hover .cta-arrow {
          transform: translateX(4px);
          animation: none;
        }

        @keyframes pulse-glow {
          0%, 100% {
            box-shadow: 0 0 5px rgba(96, 165, 250, 0.2);
            border-color: rgba(255, 255, 255, 0.1);
          }
          50% {
            box-shadow: 0 0 20px rgba(96, 165, 250, 0.4), 0 0 40px rgba(96, 165, 250, 0.2);
            border-color: rgba(96, 165, 250, 0.4);
          }
        }

        @keyframes bounce-headphones {
          0%, 100% { transform: translateY(0); }
          50% { transform: translateY(-5px); }
        }

        @keyframes arrow-move {
          0%, 100% { transform: translateX(0); }
          50% { transform: translateX(6px); }
        }
        
      </style>
    </main>

    <Footer />

    <!-- ✅ Bannière cookies -->
    <div id="cookie-banner" hidden>
      <span>Ce site utilise des cookies de mesure (Google Analytics) uniquement si vous acceptez.</span>
      <span class="actions">
        <button id="accept-cookies">Accepter</button>
        <button id="decline-cookies">Refuser</button>
      </span>
    </div>

    <!-- ✅ Scripts -->
    <script src="/scripts/consent.js" defer></script>
    <script src="/scripts/ga-init.js" defer></script>
    <script src="/scripts/downloads.js" defer></script>
  </body>
</html>
