---
import Layout from '../../layouts/BlogPost.astro';
---

<Layout
  title="Philippe de Foy – Mes livres"
  pageTitle="Mes livres"
  description="Découvrez les livres de Philippe de Foy : cours d'espagnol et de russe pour débutants (A1, A2)."
  pubDate={new Date('August 08 2024')}
  heroImage="/images/about-photo.png"
  hideBreadcrumb={true}
>

  <div class="page-container">
    <div class="intro-text">
      <p>J'ai écrit ces livres avec une idée simple : aller à l'essentiel.</p>
      <p>Pour l'apprentissage des langues, j'ai publié plusieurs méthodes pour ceux qui partent de zéro ou qui veulent consolider leurs bases : l'une pour l'apprentissage du russe (niveaux A1 et A2) et l'autre pour l'espagnol (niveau A1).</p>
      <p>Ce sont des outils concrets pour commencer à pratiquer sans se perdre dans la théorie.</p>
      <p>À côté de ça, je m'intéresse aussi à la nutrition, ce qui m'a conduit à écrire un ouvrage sur les bénéfices de consommer un œuf par jour.</p>
      <p>Ces livres sont <a href="https://www.amazon.fr/-/e/B0G92HLLH9" target="_blank" rel="noopener">disponibles sur Amazon</a> en version papier et numérique.</p>
    </div>
  </div>

  <!-- Section Recursos en español -->
  <section class="recursos-section">
    <div class="recursos-container">
      <div class="recursos-content">
        <span class="recursos-flag" aria-hidden="true">🇪🇸</span>
        <h3>Recursos en Español</h3>
        <p>¿Hablas español? Descarga audios gratuitos para mejorar tu comprensión oral.</p>
        <a href="/descargas/" class="recursos-btn" aria-label="Ir a la página de descargas en español">
          Ver recursos en español
          <span class="arrow">→</span>
        </a>
      </div>
    </div>
  </section>

  <!-- Section message personnel -->
  <section class="message-section">
    <p>Quand on apprend une langue étrangère, on a besoin d'explications claires, dans notre propre langue. C'est ce que j'essaie de vous offrir ici. Vous trouverez des leçons de grammaire, du vocabulaire, des audios pour vous entraîner. Tout est en français, du niveau débutant au niveau avancé.</p>
    <p>Vous apprenez pour un voyage ? Par passion pour la Russie, l'Espagne ou l'Amérique du Sud ? Pour parler avec quelqu'un ? Quelle que soit votre raison, les ressources sont là, gratuites et faciles à utiliser.</p>
    <p>Avancez à votre rythme. Le russe demande du temps, l'espagnol beaucoup moins, mais chaque petite étape compte.</p>
    <p class="signature">Bon apprentissage !</p>
  </section>
  </div>
</Layout>

<style>
  /* Styles spécifiques à cette page */

  /* Élargir le conteneur principal comme sur Livres */
  :global(.post-content) {
    max-width: 900px !important;
    margin: 0 auto !important;
    padding: 2rem !important;
  }

  :global(.post-content .prose) {
    max-width: 100% !important;
  }

  /* Container principal */
  .page-container {
    background: rgba(30, 41, 59, 0.4);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 2rem;
    margin-top: 1rem;
  }

  /* Réduire l'espace autour du titre */
  :global(.prose .title) {
    padding: 0.3rem 0 !important;
    margin-bottom: 0 !important;
  }
  :global(.prose .title h1) {
    font-size: 2.2rem;
    text-align: center !important;
    margin: 0 !important;
  }
  :global(.prose .title .date) {
    display: none;
  }
  :global(.prose hr) {
    margin: 0.3rem 0 !important;
  }

  /* cache le gros titre injecté par le Layout */
  h1 {
    display: none;
  }

  .intro-text {
    font-size: 1.15rem;
    color: #cbd5e1;
    line-height: 1.7;
  }

  .intro-text p {
    margin-bottom: 1rem;
  }

  .intro-text a {
    color: #6366f1;
    text-decoration: underline;
    transition: color 0.2s ease;
  }

  .intro-text a:hover {
    color: #8b5cf6;
  }

  /* === Section Recursos en español === */
  .recursos-section {
    margin: 4rem auto 0.5rem;
    padding: 0 1rem;
  }

  .recursos-container {
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.15) 0%, rgba(139, 92, 246, 0.08) 100%);
    backdrop-filter: blur(10px);
    border: 2px solid rgba(99, 102, 241, 0.4);
    border-radius: 16px;
    padding: 2.5rem 2rem;
    text-align: center;
    position: relative;
    overflow: hidden;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 12px rgba(99, 102, 241, 0.2);
  }

  .recursos-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #6366f1, #8b5cf6);
    opacity: 0.8;
  }

  .recursos-container:hover {
    border-color: rgba(139, 92, 246, 0.6);
    box-shadow: 0 12px 30px rgba(99, 102, 241, 0.3);
    transform: translateY(-2px);
  }

  .recursos-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .recursos-flag {
    font-size: 3rem;
    display: block;
    animation: wave 2s ease-in-out infinite;
    filter: drop-shadow(0 4px 12px rgba(99, 102, 241, 0.4));
  }

  @keyframes wave {
    0%, 100% { transform: rotate(0deg); }
    25% { transform: rotate(-10deg); }
    75% { transform: rotate(10deg); }
  }

  :global(body.blog .post-content .recursos-container h3) {
    font-size: 1.75rem;
    color: #f97316 !important;
    font-weight: 600;
    margin: 0;
    letter-spacing: -0.01em;
  }

  :global(.recursos-container p) {
    font-size: 1.1rem;
    color: #cbd5e1;
    margin: 0;
    max-width: 600px;
    line-height: 1.6;
  }

  :global(.recursos-btn) {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 2rem;
    background: linear-gradient(135deg, #ea580c, #f97316) !important;
    color: white !important;
    text-decoration: none;
    border-radius: 12px;
    font-weight: 600;
    font-size: 1rem;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    margin-top: 0.5rem;
    box-shadow: 0 4px 12px rgba(234, 88, 12, 0.3);
  }

  :global(.recursos-btn:hover),
  :global(.recursos-btn:focus) {
    transform: translateY(-2px) scale(1.05);
    box-shadow: 0 8px 20px rgba(234, 88, 12, 0.4);
    filter: brightness(1.1);
    outline: none;
  }

  :global(.recursos-btn:active) {
    transform: translateY(0);
  }

  :global(.arrow) {
    display: inline-block;
    transition: transform 0.3s ease;
  }

  :global(.recursos-btn:hover .arrow) {
    transform: translateX(4px);
  }

  /* Responsive */
  @media (max-width: 640px) {
    .recursos-section {
      padding: 0 1rem;
    }

    .recursos-container {
      padding: 2rem 1.5rem;
    }

    .recursos-flag {
      font-size: 2.5rem;
    }

    .recursos-container h3 {
      font-size: 1.5rem;
    }
  }

  /* === Section message personnel === */
  .message-section {
    margin: 0.5rem auto 2rem;
    padding: 1rem 2rem;
    max-width: 700px;
    text-align: center;
  }

  .message-section p {
    font-size: 1.1rem;
    color: #cbd5e1;
    line-height: 1.7;
    margin-bottom: 1rem;
  }

  .message-section .signature {
    font-weight: 600;
    color: #f1f5f9;
    font-size: 1.2rem;
    margin-top: 1.5rem;
  }
</style>